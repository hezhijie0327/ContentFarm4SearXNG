# SearXNG hostnames configuration
# This file references external rule files
# Generated by SearXNG Hostnames Generator (Enhanced - Fixed TLD Issues + Precise Whitelist)
# Multi-rule performance optimized with advanced pattern matching
# Fixed: TLD optimization preserves complete domain structure (.pixnet.net vs .pixnet)
# Smart domain sorting and TLD grouping applied
# Precise whitelist filtering enabled for exact control

hostnames:
  remove: remove-hosts.yml
